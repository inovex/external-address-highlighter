{
  "name": "external-address-highlighter",
  "version": "1.1.1",
  "description": "Highlights mail-recipients with external addresses.",
  "author": "inovex",
  "license": "UNLICENSED",

  "scripts": {
    "web-ext": "web-ext",
    "start": "web-ext run",
    "serve": "python -m SimpleHTTPServer",
    "lint": "web-ext lint",
    "build": "web-ext build",
    "sign": "web-ext sign"
  },

  "devDependencies": {
    "web-ext": "^2.3.1"
  }
}
